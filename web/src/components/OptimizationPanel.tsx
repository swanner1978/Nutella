export function OptimizationPanel() {
  return (
    <section
      style={{
        marginTop: "1.5rem",
        padding: "1rem",
        border: "1px solid #333",
        borderRadius: "4px",
        backgroundColor: "#0a0a0a",
      }}
    >
      <h2 style={{ margin: "0 0 0.5rem", fontSize: "1rem", color: "#fff" }}>Optimisation</h2>
      <p style={{ margin: 0, color: "#666", fontSize: "0.875rem" }}>
        Panneau d&apos;optimisation — squelette UI (logique métier non implémentée).
      </p>
    </section>
  );
}
